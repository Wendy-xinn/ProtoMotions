diff --git a/pyproject.toml b/pyproject.toml
index 97a50399e..3af1f5ab3 100644
--- a/pyproject.toml
+++ b/pyproject.toml
@@ -41,8 +41,8 @@ dependencies = [
     # pytetwild: keep the behavior-stable 0.2.3 pin on x86_64. 0.3.0 added manylinux
     # aarch64 wheels; it imports pyvista at import time but only declares it in its
     # [all] extra, so depend on that extra there.
-    "pytetwild==0.2.3 ; platform_machine in 'x86_64 AMD64'",
-    "pytetwild[all]>=0.3.0,<0.4 ; platform_machine in 'aarch64 arm64'",
+    "pytetwild==0.2.3 ; platform_machine == 'x86_64' or platform_machine == 'AMD64'",
+    "pytetwild[all]>=0.3.0,<0.4 ; platform_machine == 'aarch64' or platform_machine == 'arm64'",
     "transformers==4.57.6",
     "einops",
     "warp-lang==1.15.0.dev20260626",
@@ -124,7 +124,7 @@ rerun = [
 # Isaac Sim (PhysX backend); co-resolves with the base install via the [tool.uv] conflicts table.
 isaacsim = ["isaacsim[all,extscache]==6.0.0.1"]
 # Omniverse renderer / physics backends (``ov[ovphysx|ovrtx|all]`` selectors).
-ov = ["ovphysx==0.5.2+head.f62c22207c"]
+ov = ["ovphysx==0.5.10"]
 rtx = ["ovrtx>=0.3.0,<0.4.0"]
 mimic = [
     "isaaclab-mimic",
@@ -165,7 +165,7 @@ isaacsim = "6.0.0.1"
 torch = "2.11.0"
 torchvision = "0.26.0"
 torchaudio = "2.11.0"
-ovphysx = "0.5.2+head.f62c22207c"
+ovphysx = "0.5.10"
 ovrtx = ">=0.3.0,<0.4.0"
 # Newton git commit + matching warp-lang prerelease (upgraded together).
 newton = "c7ae7c7648cd0717df39e5c94b95d5a02c997320"
diff --git a/source/isaaclab_physx/isaaclab_physx/assets/rigid_object/rigid_object_data.py b/source/isaaclab_physx/isaaclab_physx/assets/rigid_object/rigid_object_data.py
index def3c6a9c..7f4f81ff4 100644
--- a/source/isaaclab_physx/isaaclab_physx/assets/rigid_object/rigid_object_data.py
+++ b/source/isaaclab_physx/isaaclab_physx/assets/rigid_object/rigid_object_data.py
@@ -258,9 +258,9 @@ class RigidObjectData(BaseRigidObjectData):
                 shared_kernels.get_root_link_vel_from_root_com_vel,
                 dim=self._num_instances,
                 inputs=[
-                    self.root_com_vel_w,
-                    self.root_link_pose_w,
-                    self.body_com_pose_b,
+                    self.root_com_vel_w.warp,
+                    self.root_link_pose_w.warp,
+                    self.body_com_pose_b.warp,
                 ],
                 outputs=[
                     self._root_link_vel_w.data,
@@ -287,8 +287,8 @@ class RigidObjectData(BaseRigidObjectData):
                 shared_kernels.get_root_com_pose_from_root_link_pose,
                 dim=self._num_instances,
                 inputs=[
-                    self.root_link_pose_w,
-                    self.body_com_pose_b,
+                    self.root_link_pose_w.warp,
+                    self.body_com_pose_b.warp,
                 ],
                 outputs=[
                     self._root_com_pose_w.data,
